# Python - Almost a Circle

This project implements the `Base`, `Rectangle`, and `Square` classes and their
JSON serialization, file persistence, update, and dictionary-conversion
features.

## Run the tests

```bash
python3 -m unittest discover tests
```

## Run pycodestyle

```bash
pycodestyle models tests
```

